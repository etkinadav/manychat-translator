/**
 * Default Vite config — builds content script only.
 * Full extension build uses both vite.content.config.ts and
 * vite.background.config.ts (see package.json scripts).
 */
export { default } from "./vite.content.config";
