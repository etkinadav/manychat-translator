# Google Cloud (Translate + Vertex AI Gemini) — ADC: gcloud auth application-default login
# Real GCP project ID (e.g. screen-translator-496203) — NOT the literal placeholder below
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=us-central1

# Chat under messages: SOURCE → TARGET (e.g. he → en).
# Composer "Translate to Hebrew" button: TARGET → SOURCE (reverse).
TRANSLATE_TARGET_LANGUAGE=en
TRANSLATE_SOURCE_LANGUAGE=he

# Same MongoDB as beams/backend/.env — must include DB name (e.g. node-angular), not only the cluster host
MONGO_URI=mongodb+srv://USER:PASS@cluster....mongodb.net/node-angular?retryWrites=true&w=majority

# Same JWT secret as beams backend so tokens are compatible
JWT_KEY=

# Backend port (default 3000)
PORT=3000

# Comma-separated Vite dev origins allowed by CORS
FRONTEND_DEV_ORIGINS=http://localhost:5173

# Outgoing "Translate to …" uses Vertex AI Gemini (ADC, no API key)
# true = log full prompt only, return original text unchanged
GEMINI_OUTGOING_DRY_RUN=false
